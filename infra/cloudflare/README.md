# Cloudflare boundary

This directory records a possible later infrastructure boundary. Part 4
documents Cloudflare secret storage and rotation, but no account, R2 bucket,
credential, binding, always-on service, or deployment is configured. See
`docs/operations/secret-storage-and-rotation.md`.
